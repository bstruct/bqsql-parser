SELECT
    1 AS number,
    "2+2" AS string,
    2+2 AS calculation,
    1,
    2,
    3,
    false as bool,
    ST_GEOGPOINT(-50, 90) geo_point,
    ST_GEOHASH(ST_GEOGPOINT(-50, 90), 5) geohash,
    ST_GEOGFROMTEXT('LINESTRING(1 2, 3 4)') AS geoline,
    4;

SELECT ALL
    1,
    2;

SELECT AS STRUCT
    (
        1,2,3
    ) AS points;

SELECT (2,3,4.) AS x;

SELECT ((2,3,4)) AS x;

SELECT (((2,3,4))) AS x;
